Place your logo image in this folder with the filename `medfinder_logo.png`.

Recommended: use the provided logo (transparent background, high resolution). For best results use a square PNG (at least 512x512). After adding the file, run:

```
flutter pub get
flutter clean
flutter run
```
